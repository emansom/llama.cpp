%llguidance {}

# Qwen3.5 chat grammar (Lark / llguidance)
#
# Wire format for the model's emitted assistant turn (after the template's
# generation prompt `<|im_start|>assistant\n<think>\n` has been emitted):
#   <reasoning></think>\n\n<content>(<tool_call>\n<function=NAME>\n
#     (<parameter=K>\nVALUE\n</parameter>\n)*</function>\n</tool_call>)*
#
# Like Qwen3-Coder this is a per-arg XML format: each arg is wrapped in its own
# <parameter=K>...</parameter> block rather than a JSON args object.
#
# Two named entries:
#   - `start` (default) --- assistant-turn continuation; sampler entry.
#   - `conversation` --- full conversation wire shape; selected via arena
#     set_root() by the bidirectional walker.

start: assistant_body

# --- Assistant turn continuation (sampler entry) -------------------------------
assistant_body: think_block content tool_call_block?

# Accept both `<think>\n...` (Qwen3.5 thinking-on generation prompt) and
# `<think>...` without a forced newline (Nemotron thinking-off generation
# prompt is `<think></think>`).
think_block: "<think>" reasoning "</think>"
reasoning: /(.|\n)*?/

content: /([^<]|<(?!tool_call>))*/

tool_call_block: tool_call (tool_call_sep tool_call)*
tool_call_sep: "\n"
tool_call: "<tool_call>\n<function=" func_name ">\n" parameter* "</function>\n</tool_call>"

func_name: FUNC_NAME
FUNC_NAME: /[a-zA-Z_][a-zA-Z0-9_]*/

parameter: "<parameter=" arg_name ">\n" arg_value "\n</parameter>\n"
arg_name: ARG_NAME
ARG_NAME: /[a-zA-Z_][a-zA-Z0-9_]*/
arg_value: /([^<]|<(?!\/parameter>))*/

# --- Full-conversation walker entry --------------------------------------------
conversation: turn+ assistant_open?
turn: system_turn | user_turn | tool_turn | assistant_full_turn
system_turn:         "<|im_start|>system\n"    turn_body_text "<|im_end|>\n"
user_turn:           "<|im_start|>user\n"      turn_body_text "<|im_end|>\n"
tool_turn:           "<|im_start|>tool\n"      turn_body_text "<|im_end|>\n"
assistant_full_turn: "<|im_start|>assistant\n" turn_body_text "<|im_end|>\n"
assistant_open:      "<|im_start|>assistant\n"

turn_body_text: /([^<]|<(?!\|im_end\|))*/
